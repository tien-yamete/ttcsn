server:
  port: 8080                  # Port server ch?y
  servlet:
    context-path: /identity   # Context path chung cho API

spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/identity_service}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:814362}
    driver-class-name: "com.mysql.cj.jdbc.Driver"
  jpa:
    hibernate:
      ddl-auto: update   # T? t?o/t? update b?ng DB theo entity
    show-sql: true       # Log SQL ra console
  kafka:
    bootstrap-servers: localhost:9094
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

jwt:
  signerKey: "dDQUxe/9TmZRTqCPATFEHWkNLKmAMsgl0rdQGa2AIjW1J7dj2dAEsNyZVhfjT5rS" # Khóa ký JWT
  issuer: "tavantien.com"     # Issuer c?a JWT
  valid-duration: 3600 # in second
  refreshable-duration: 36000 # in seconds
app:
  services:
    profile: http://localhost:8081/profile